services:
  - type: web
    name: ai-planner-cloud
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: "20"
    healthCheckPath: /api/ping
